openapi: 3.1.0
info:
  title: API Sucursales Restaurante
  description: |
    Especificación viva de los tres flujos de endpoints. La UI interactiva está en `/docs/api`.
    Ramas sugeridas: `flow/sucursales`, `flow/catalogo`, `flow/pedidos`.
  version: 0.1.0
servers:
  - url: /
    description: Servidor local (php artisan serve)
tags:
  - name: Sucursales
    description: Flujo `flow/sucursales` — ubicaciones, horarios, datos de sucursal.
  - name: Catálogo
    description: Flujo `flow/catalogo` — menú, categorías, precios, disponibilidad.
  - name: Pedidos
    description: Flujo `flow/pedidos` — órdenes, estados, pagos.
  - name: Stock
    description: Flujograma 3 — gestión de stock e ingredientes al registrar pedidos (movimientos, alertas, órdenes de compra, bloqueo de menú).

paths:
  /api/v1/sucursales/health:
    get:
      tags: [Sucursales]
      summary: Health del flujo sucursales
      operationId: sucursalesHealth
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  flow:
                    type: string
                    example: sucursales
                  message:
                    type: string

  /api/v1/catalogo/health:
    get:
      tags: [Catálogo]
      summary: Health del flujo catálogo
      operationId: catalogoHealth
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  flow:
                    type: string
                    example: catalogo
                  message:
                    type: string

  /api/v1/orders:
    post:
      tags: [Stock, Pedidos]
      summary: Registrar pedido (dispara Flujograma 3 — stock)
      description: |
        Equivalente funcional a `POST /api/v1/pedidos`. Si el ítem coincide con `producto_ingrediente` (ej. "Pizza"),
        descuenta stock, registra `inventario_movimientos`, evalúa umbral y puede generar alertas y órdenes de compra.
      operationId: ordersStore
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoCreateRequest'
      responses:
        '201':
          description: Pedido creado — stock OK o alerta crítica (ver cuerpo `stock`)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoCreadoConStock'
        '207':
          description: Pedido creado con alerta warning de stock (Multi-Status)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoCreadoConStock'
        '401':
          description: No autenticado
        '422':
          description: Error de validación

  /api/v1/orders/purchase:
    post:
      tags: [Stock]
      summary: Orden de compra sugerida
      operationId: ordersPurchaseSuggested
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sucursal, ingrediente_id, cantidad_sugerida]
              properties:
                sucursal: { type: string, example: Santa Tecla }
                ingrediente_id: { type: integer, example: 1 }
                cantidad_sugerida: { type: number, example: 25 }
                pedido_id: { type: integer, nullable: true }
      responses:
        '201':
          description: Orden sugerida registrada
        '401':
          description: No autenticado

  /api/v1/purchase-orders:
    post:
      tags: [Stock]
      summary: Orden de compra urgente
      operationId: purchaseOrdersUrgent
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sucursal, ingrediente_id, cantidad_sugerida]
              properties:
                sucursal: { type: string }
                ingrediente_id: { type: integer }
                cantidad_sugerida: { type: number }
                pedido_id: { type: integer, nullable: true }
      responses:
        '201':
          description: Orden urgente registrada
        '403':
          description: Solo gerente o admin

  /api/v1/inventory/branch/{sucursal}:
    get:
      tags: [Stock]
      summary: Stock por sucursal (suma lógica por ítem)
      operationId: inventoryByBranch
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: sucursal
          required: true
          schema: { type: string }
          description: Nombre de sucursal (URL-encoded si lleva espacios)
      responses:
        '200':
          description: Listado de stock por ingrediente en la sucursal
        '401':
          description: No autenticado

  /api/v1/analytics/stock-alerts:
    get:
      tags: [Stock]
      summary: Alertas de stock
      operationId: analyticsStockAlerts
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: sucursal
          schema: { type: string }
        - in: query
          name: nivel
          schema: { type: string, enum: [warning, critical] }
      responses:
        '200':
          description: Paginado de alertas
        '401':
          description: No autenticado

  /api/v1/pedidos/health:
    get:
      tags: [Pedidos]
      summary: Health del flujo pedidos
      operationId: pedidosHealth
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  flow:
                    type: string
                    example: pedidos
                  message:
                    type: string

  /api/v1/pedidos:
    get:
      tags: [Pedidos]
      security:
        - bearerAuth: []
      summary: Listar pedidos
      operationId: pedidosIndex
      parameters:
        - in: query
          name: estado
          schema: { type: string, enum: [pendiente, en_preparacion, listo, entregado, cancelado] }
        - in: query
          name: canal
          schema: { type: string, enum: [salon, delivery, take-away] }
        - in: query
          name: sucursal
          schema: { type: string }
      responses:
        '200':
          description: Lista paginada de pedidos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Pedido'
    post:
      tags: [Pedidos, Stock]
      security:
        - bearerAuth: []
      summary: Crear pedido (dispara Flujograma 3 si hay receta)
      operationId: pedidosStore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoCreateRequest'
      responses:
        '201':
          description: Pedido creado — stock OK o crítico (ver `stock`)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoCreadoConStock'
        '207':
          description: Pedido creado con alerta warning de stock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoCreadoConStock'
        '422':
          description: Error de validación

  /api/v1/pedidos/{id}:
    get:
      tags: [Pedidos]
      security:
        - bearerAuth: []
      summary: Obtener detalle de pedido
      operationId: pedidosShow
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Pedido encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pedido'
        '404':
          description: Pedido no encontrado
    delete:
      tags: [Pedidos]
      security:
        - bearerAuth: []
      summary: Eliminar pedido
      operationId: pedidosDestroy
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '204':
          description: Pedido eliminado
        '404':
          description: Pedido no encontrado
        '409':
          description: Conflicto por estado entregado

  /api/v1/pedidos/{id}/estado:
    patch:
      tags: [Pedidos]
      security:
        - bearerAuth: []
      summary: Cambiar estado del pedido
      operationId: pedidosUpdateEstado
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [estado]
              properties:
                estado:
                  type: string
                  enum: [pendiente, en_preparacion, listo, entregado, cancelado]
      responses:
        '200':
          description: Estado actualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pedido'
        '422':
          description: Transición no válida o error de validación
        '404':
          description: Pedido no encontrado

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Sanctum
      description: Token devuelto por `POST /api/v1/auth/login`

  schemas:
    StockPayload:
      type: object
      properties:
        estado:
          type: string
          enum: [ok, warning, critical]
        alertas:
          type: array
          items:
            type: object
        ordenes_compra:
          type: array
          items:
            type: object
        items_menu_bloqueados:
          type: array
          items:
            type: object
        notificaciones:
          type: array
          items:
            type: object

    PedidoCreadoConStock:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/Pedido'
        stock:
          $ref: '#/components/schemas/StockPayload'

    Pedido:
      type: object
      properties:
        id: { type: integer, example: 1 }
        codigo: { type: string, example: PED-20260408-001 }
        cliente_nombre: { type: string, nullable: true, example: Juan Pérez }
        canal: { type: string, enum: [salon, delivery, take-away] }
        sucursal: { type: string, example: San Salvador Centro }
        estado: { type: string, enum: [pendiente, en_preparacion, listo, entregado, cancelado] }
        total: { type: number, format: float, example: 25.50 }
        items:
          type: array
          items:
            type: object
            properties:
              nombre: { type: string, example: Hamburguesa Clásica }
              cantidad: { type: integer, example: 2 }
              precio_unitario: { type: number, format: float, example: 8.75 }
        notas: { type: string, nullable: true, example: Sin cebolla }
        created_by: { type: integer, nullable: true, example: 5 }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    PedidoCreateRequest:
      type: object
      required: [canal, sucursal, items]
      properties:
        cliente_nombre: { type: string, example: Juan Pérez }
        canal: { type: string, enum: [salon, delivery, take-away] }
        sucursal: { type: string, example: San Salvador Centro }
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [nombre, cantidad, precio_unitario]
            properties:
              nombre: { type: string, example: Hamburguesa Clásica }
              cantidad: { type: integer, minimum: 1, example: 2 }
              precio_unitario: { type: number, minimum: 0, example: 8.75 }
        notas: { type: string, example: Sin cebolla }
